{
  "version": 2,
  "installCommand": "npm install",
  "buildCommand": "cd packages/shared && npm run build && cd ../templates && npm run build && cd ../../apps/api && npm run build",
  "functions": {
    "apps/api/src/index.ts": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "apps/api/src/index.ts"
    }
  ]
}
